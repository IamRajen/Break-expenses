<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.expenses.breakexpenses">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginPage" />
        <activity android:name=".SigninPage" />
        <activity android:name=".UserDetails" />
        <activity android:name=".VerifyPhoneNumber" />
        <activity
            android:name=".HomePage"
            android:label="@string/title_activity_homepage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TripFragment" />
        <activity android:name=".FriendFragment" />
        <activity android:name=".PlanATrip" />
        <activity android:name=".TripModification" />
        <activity android:name=".ExpenseModification" />
        <activity android:name=".ManageFriendExpense"></activity>
    </application>

</manifest>